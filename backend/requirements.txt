# Python version 3.11.14

# FastAPI and server
fastapi
uvicorn[standard]
python-multipart
websockets

# AWS SDK
boto3
botocore

# Voice Processing
azure-cognitiveservices-speech  # Azure Speech SDK — persistent connection, same Neerja voice, ~50-200ms/sentence

# Utilities
python-dotenv
pydantic
httpx  # Async HTTP client (Deepgram STT API)
pdfplumber  # PDF text extraction — primary (layout-aware, handles multi-column)
pypdf  # Fallback PDF extraction (used inside pdfplumber and as last resort)

# Auth & Database
PyJWT>=2.8.0     # Supabase JWT verification
cryptography>=3.4  # ES256/RS256 support for PyJWT (Supabase uses ES256)
asyncpg>=0.29.0  # Async PostgreSQL driver (Supabase)

# Rate limiting
slowapi>=0.1.9   # FastAPI rate limiting (built on limits)